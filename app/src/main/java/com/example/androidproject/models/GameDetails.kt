package com.example.androidproject.models

class GameDetails (gameId: String, title: String, editor: String, price: Double, logo: String, cover: String)
    : GameResume(gameId, title,
    editor,
    price
) {
}